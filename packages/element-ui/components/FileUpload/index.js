import { withInstall } from '../../utils'
import FileUpload from './src/index.vue'
// 使用 withInstall 注册组件并导出组件
export const XFileUpload = withInstall(FileUpload)
// 使用 withInstall 注册组件并导出组件
export default XFileUpload
