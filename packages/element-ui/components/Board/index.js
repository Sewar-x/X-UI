import { withInstall } from '../../utils'
import Board from './src/index.vue'
// 使用 withInstall 注册组件并导出组件
export const XTaskBoard = withInstall(Board)
// 使用 withInstall 注册组件并导出组件
export default XTaskBoard
