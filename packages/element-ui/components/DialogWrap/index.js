import { withInstall } from '../../utils'
import dialogWrap from './src/index.vue'
// 使用 withInstall 注册组件并导出组件
export const DialogWrap = withInstall(dialogWrap)
// 使用 withInstall 注册组件并导出组件
export default DialogWrap
