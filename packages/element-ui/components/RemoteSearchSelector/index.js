import { withInstall } from '../../utils'
import remoteSearchSelector from './src/index.vue'
// 使用 withInstall 注册组件并导出组件
export const RemoteSearchSelector = withInstall(remoteSearchSelector)
// 使用 withInstall 注册组件并导出组件
export default RemoteSearchSelector
