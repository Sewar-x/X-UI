import { withInstall } from '../../utils'
import selectorWrap from './src/index.vue'
// 使用 withInstall 注册组件并导出组件
export const SelectorWrap = withInstall(selectorWrap)
// 使用 withInstall 注册组件并导出组件
export default SelectorWrap
