import { withInstall } from '../../utils'
import xtinymce from './src/index.vue'
// 使用 withInstall 注册组件并导出组件
export const Tinymce = withInstall(xtinymce)
// 使用 withInstall 注册组件并导出组件
export default Tinymce
