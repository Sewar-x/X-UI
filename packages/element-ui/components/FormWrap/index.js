import { withInstall } from '../../utils'
import formWrap from './src/index.vue'
// 使用 withInstall 注册组件并导出组件
export const FormWrap = withInstall(formWrap)
// 使用 withInstall 注册组件并导出组件
export default FormWrap
