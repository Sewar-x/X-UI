import { withInstall } from '../../utils'

import Loading from './src/Loading.vue'
// 使用 withInstall 注册组件并导出组件
export const XLoading = withInstall(Loading)
// 使用 withInstall 注册组件并导出组件
export default XLoading

