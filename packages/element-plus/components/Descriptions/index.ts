import { withInstall } from '../../utils'
import Descriptions from "./src/XDescriptions.vue";
// 使用 withInstall 注册组件并导出组件
export const XDescriptions = withInstall(Descriptions)
// 使用 withInstall 注册组件并导出组件
export default XDescriptions

