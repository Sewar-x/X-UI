import { withInstall } from '../../utils'
import BasicPopoverVue from "./src/BasicPopover.vue";

// 使用 withInstall 注册组件并导出组件
export const XPopover = withInstall(BasicPopoverVue)
// 使用 withInstall 注册组件并导出组件
export default XPopover
